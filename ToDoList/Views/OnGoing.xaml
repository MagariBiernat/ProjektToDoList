<UserControl x:Class="ToDoList.Views.OnGoing"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ToDoList.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Loaded="Window_Loaded"
             >
    <Grid>
        <DataGrid AutoGenerateColumns="False"  x:Name="OnGoingTasks">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Id}" Width="50" Header="ID"/>
                <DataGridTextColumn Binding="{Binding text}" Width="200" Header="Nazwa zadania"/>
                <DataGridTextColumn Binding="{Binding data}" Width="100" Header="Data i godzina"/>
                <DataGridTextColumn Binding="{Binding priority}" Width="80" Header="Waga zadania"/>
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</UserControl>
